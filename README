Sample application to show a working example of the translate_routes plugin.

The application interface includes file-by-file instructions, so just install the translate_routes plugin under vendor/plugins and start the application it with script/server to see it working. 